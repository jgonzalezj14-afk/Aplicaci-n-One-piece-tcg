import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('Test inicial', (WidgetTester tester) async {
    // Este test no hace nada, pero es válido.
    // Más adelante podrás crear tests para tu app aquí.
    expect(1 + 1, 2);
  });
}